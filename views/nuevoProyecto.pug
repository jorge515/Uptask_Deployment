extends layout

include mixin/crearProyecto

block contenido
   .contenido-principal
      h1= #{nuevoProyecto}
      pre= vardump(proyecto)

      if errores 
        each  error in errores 
          .alerta= error.texto
      +crearProyecto(proyecto)

     